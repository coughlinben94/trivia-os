# Plan Review Log: Nightly Round-Journey Designer Agent
Act 1 (grill) complete — plan locked with the user. MAX_ROUNDS=5.

## Round 1 — Codex (thread 019f80da-a727-7451-86b1-aa4e0ff043f1)
28 findings, VERDICT: REVISE. Full critique preserved at /tmp/codex-verdict.txt on host; key items summarized in Claude's response below (findings numbered as Codex gave them).

### Claude's response
**Accepted (plan revised):**
- #2/#3/#28 Gallery serving: dropped manifest.json fetch + Vercel assumption entirely. Gallery now uses `manifest.js` (JS global, same pattern as Ben's fact-feed) so double-clicking index.html works under file://. Vercel deployment moved to explicit out-of-scope. Deployment question resolved now, not deferred.
- #4 External sprite URLs: SVG must be embedded inline in the prototype (or committed local file); img.recraft.ai hotlinks rejected at audit.
- #5 SVG sanitization: strip script/event-handler attrs/foreignObject/external refs before embedding; fail closed.
- #1 (partial) iframe sandbox="allow-scripts" in gallery — cheap, taken; threat framing (Supabase credential theft) rejected as inapplicable since gallery stays off-origin/local.
- #7 (partial)/#8/#9 Git safety: preflight + hard guard — agent stages only `concepts/` paths; aborts commit if it modified anything outside; skips push (logs instead) if unpushed non-nightly commits sit on main. Worktree/PR/scoped-credential infra rejected as overhead for this scale.
- #10/#11 Queue state machine: entries get id + `raw→grilled→building→built|blocked|rejected`; agent claims by writing `building` first; stale `building` at next preflight = crashed run, marked blocked. Statuses unified; blocked entries have nullable file.
- #12 (partial)/#27 Manifest gains: id, journeyType, fromTheme/toTheme, degraded flag, sprite provenance (prompt+model+date). schemaVersion/JSON-Schema validation rejected.
- #15 Cross-theme handoff shape now representable (journeyType + from/to).
- #16 Read list: + themes/index.js and relevant ParticleBackground engine when journey touches an existing theme; gsap-core/gsap-timeline/emil-design-eng/impeccable skill loads made mandatory preconditions per round-journeys.md.
- #17/#18 Credit cap redefined as max 10 generation CALLS/night (countable); fallback sprites mark manifest `degraded: true`, surfaced in gallery, not approvable until regenerated.
- #19 (partial) Audit claims reframed honestly: static code-invariant verification only; runtime feel/banding/TV behavior explicitly remain Ben's morning + venue check. Headless-browser test farm rejected — tonight's session proved this sandbox cannot run one; honesty over infrastructure.
- #21/#22 (light) Checkpoint = claim-commit early (queue `building` + log entry); next-run preflight detects stale claims. Staging-dir/atomic-publish machinery rejected.
- #24 Preflight added: repo present, references readable, Recraft reachable (get_user), queue parseable — all before claim or spend; fail = skip-and-log.
- #25 Gallery lazy-loads and pauses offscreen iframes.
- #26 Feedback path defined: Ben reviews verbally in a daytime session; Claude updates manifest + LESSONS.md — no hand-edited JSON.

**Rejected (with reasons):**
- #6 Structural approval enforcement (protected branches, signed review commits): process theater for a single-reviewer hobby pipeline; the real gate is that nothing enters the app without Ben hand-promoting it into SlideRenderer.jsx, which the agent cannot do by prompt AND has no path to do accidentally.
- #13/#14 Restrict to existing 21 themes: contradicts Ben's explicit instruction ("those are old news"). Mitigated instead: agent-proposed themes must define concrete palette hex tokens in the brief (satisfies the real-material rule in spirit) and are labeled speculative — their promotion path explicitly includes human theme-creation work.
- #20 Fixed-step accumulator spec: existing dtn convention is the codebase's own documented precedent; adding visibility-change clock reset as a one-line audit item, not a new timing architecture.
- #23 Heartbeat/scheduler monitoring: scheduled-task sessions are visible in Cowork's sidebar; a silent-failure morning is detectable by absence of a gallery update. Accepted residual risk.

## Round 2 — Codex (same thread)
8 findings, VERDICT: REVISE. "Most prior findings are addressed or reasonably rejected. Material operational gaps remain."

### Claude's response
**All 8 accepted — concrete and cheap:**
1. Claim is now an immediate COMMIT at step 3c (crash-durable for real), completion is a second commit.
2. Repo-local lockfile `concepts/.nightly-lock` (runId, session identity, start time, 4h expiry, atomic create); only lock owner classifies its own claim; stale = past expiry only.
3. Preflight records `git status --porcelain` baseline; out-of-scope guard fires only on DELTA from baseline, not on Ben's pre-existing dirty files.
4. Push guard: fetch origin first, explicit `git push origin main:main`, re-check divergence immediately before push, non-fast-forward = logged safe failure.
5. Staging by explicit per-run file allowlist (files this run created/changed), never a blanket `git add concepts/`.
6. manifest.js written only as `window.MANIFEST = <JSON.stringify output>;` (single assignment, no expressions); gallery renders every field via textContent.
7. postMessage play/pause/replay contract added to prototype conventions (origin check, rAF cancel, clock reset, animation-play-state); audited per prototype.
8. SVG sanitization is a concrete committed script (`concepts/tools/sanitize-svg.mjs`, parser-based element/attribute allowlist) with adversarial fixtures that must be rejected during the supervised first run.

## Round 3 — Codex (same thread)
5 findings, VERDICT: REVISE. All accepted:
1. Lock now held through validation/commit/push/cleanup, released as final step even on failure paths.
2. Claim commit uses the same single guarded-push routine as completion (fetch → divergence → baseline → explicit push).
3. Baseline is content hashes (SHA-256) of out-of-scope dirty/untracked files, not porcelain-line comparison.
4. Sanitizer gets a real vendored XML parser via self-contained concepts/tools/package.json (installed once at build-out; root package files untouched; regex fallback forbidden).
5. postMessage validation = exact event.source + per-frame nonce + strict schema; origin never trusted alone (file:// frames report "null").

Side note: Gemini cross-review requested by Ben — blocked on auth (no GEMINI_API_KEY / OAuth on this machine). Pending Ben running `npx @google/gemini-cli` once interactively.

## Round 4 — Codex (same thread)
VERDICT: APPROVED. Five non-blocking implementation notes, all folded into PLAN.md: committed package-lock + preflight `npm ci --prefix concepts/tools`; nonce via URL fragment before src; document-level pause class (not root-only animation-play-state); expiry takeover named as the explicit exception to owner-only-classifies; subtitle updated.

**Converged: 4 rounds, 28 + 8 + 5 findings processed. Plan approved by Codex; awaiting Ben's final sign-off. Gemini cross-review still pending auth.**

## Gemini cross-review round 1 (manual relay — Ben pasted PLAN.md into Gemini, pasted findings back)
7 findings, VERDICT: REVISE.

### Claude's response
**Accepted (5, plan revised):**
1. LESSONS.md unbounded growth → capped at 10 most-recent active directives; older entries auto-summarized into a standing "established conventions" block rather than deleted outright.
2. Manual/scheduled trigger race → made explicit: both triggers run the identical preflight including `.nightly-lock` acquisition; no separate lighter-weight manual path exists.
3. SVG entity-expansion/billion-laughs + node-count DoS → sanitizer now explicitly rejects `<!ENTITY` declarations and enforces max byte-size + max node-depth before parsing; adversarial fixtures extended to cover these.
6. Partial sprite batch mixing Recraft + primitives in one scene → full batch generated before any HTML; cap/failure mid-batch discards the whole batch and downgrades the entire prototype to primitives, never a mix.
7. manifest.js non-atomic write → write to `manifest.js.tmp`, rename over the live file.

**Rejected (2, with reasons):**
4. "Delete dtn math, use GSAP lagSmoothing exclusively" → false dichotomy: dtn normalization governs canvas/rAF-driven pieces (particle fields, warp-engine extensions) and is explicitly the app's own documented precedent (round-journeys.md's "Reusable lessons," copied verbatim from TeamPickerSlide.jsx) — not something this plan invented to fight GSAP. GSAP-timeline-driven pieces should and do use GSAP's own lagSmoothing, per the skill's own conventions; the two mechanisms serve different piece types and were never meant to compete. Clarified in plan rather than replaced.
5. "Extract theme-tokens.json to avoid feeding raw ParticleBackground.jsx" → round-journeys.md's own input-requirements section mandates reading the real existing engine file specifically so a new motif extends real code rather than an invented generic shape; this is a read-only reference load each run, not a growing cost or an app-source change (which is out of scope for this agent regardless). Token cost is real but one-time per run, not a liability worth an app refactor to avoid.

## Gemini cross-review round 2 (manual relay)
VERDICT: APPROVED. Confirmed both rejections sound (dtn/GSAP separation of concerns; read-only ParticleBackground.jsx load respects the client/src write boundary). Confirmed all 5 incorporated fixes as tightly specified.

**FULLY CONVERGED — both Codex (4 rounds, 41 findings) and Gemini (2 rounds, 7 findings) approve. Plan locked pending Ben's final sign-off.**

## Fable contrarian pass (subagent, model: fable) — on AGENT-PROMPT.md, creative-quality review
Requested by Ben to stay in the loop as a contrarian creative-director voice, same role it played in tonight's parallel session. Unlike Codex/Gemini (mechanics: security, races, atomicity), this pass targeted a question nobody had asked yet: will this actually produce good creative work night after night, or technically-safe checklist slop? Fable read the real files (round-journeys.md, QUEUE.md, existing prototype HTMLs from tonight) rather than reasoning from the pasted spec alone. 8 findings.

### Claude's response
**Accepted (7, AGENT-PROMPT.md/QUEUE.md/PLAN.md/LESSONS.md revised):**
1. No anti-repetition memory → Step 1 gains a mandatory check: read manifest.js entries + last 5 prototypes' notes blocks; tonight's piece must differ in setting, palette family, and emotional register; one-line "what's distinct" note required.
2. No total-runtime budget → 8–14s duration target (including tableau) added to Step 4's build requirements and Step 5's audit (overlong = audit failure, cut content not just speed up).
3. Craft additions already a visible house formula, evidenced against real files (meteor-shower-windshield-journey.html / meteor-liftoff-pov already share identical grain + repeated letterbox) → split into 2 mandatory-hygiene items (grain, TV legibility) + a 4-item menu (pick ≥2, log why); letterbox capped to ~1 in 3 uses, not default.
5. Self-grill checks completeness not quality/"the turn" → invent-mode now requires 3 candidate premises with 2 killed in writing, and an explicit "the turn is ___" line before development proceeds.
6. Sprite-per-element can't deliver "what changes" (the actual scene-checklist payoff) → Step 3 rebudgeted to sprite-per-STATE for the element that changes, not one-per-noun by default.
7. Feedback only accumulates as don'ts (rejections → LESSONS.md; approvals → nothing) → PLAN.md §5 approve path now requires a one-line "why it landed" note, appended to LESSONS.md's Established Conventions as a positive pattern; LESSONS.md header updated to reflect both sources.
8. Real doc contradiction I introduced: QUEUE.md said agent-proposed concepts skip the file entirely; AGENT-PROMPT.md Step 2(3) said append them as `building`. Fixed QUEUE.md to match AGENT-PROMPT's version (durable record — also what finding #2's anti-repetition check reads).

**Flagged, not unilaterally resolved (1):**
4. Fable's core structural finding — that invent-mode (new worlds, not existing themes) produces orphan journeys no real round can ever play until Ben builds an entire new shipped theme — is real and well-argued, but its proposed fix ("invent nights should target existing themes, not new worlds") directly contradicts Ben's own explicit instruction earlier this session ("not from the 21, those are kind of old news, we're on a new in depth plan here"). Not overridden either direction — AGENT-PROMPT.md Step 2(3) now states the real cost plainly (pure speculative concept art, not show-ready, requires a full new theme build before it can ever ship) so the tradeoff is visible rather than silently assumed away. Surfaced to Ben directly for his call, consistent with how genuine conflicts with his explicit direction have been handled throughout (see Round 1 #13/#14 rejection, same pattern).

## Codex code-level review (round 5, same thread) — implementation, not plan
Requested after full build-out (gallery, sanitizer, postMessage contract, AGENT-PROMPT.md, QUEUE/LESSONS/NIGHTLY-LOG). 18 findings, all real, none rejected. Every fix was written AND tested against real adversarial input / real disposable git repos before being trusted — not patched blind.

### Claude's response — all 18 accepted and fixed, each verified:
1. `git commit` (no pathspec restriction) could sweep in Ben's other staged changes → `guarded-commit-push.sh` uses `git commit --only -- <paths>`. Verified directly: staged an out-of-scope README.md change, confirmed it was NOT swept into a `--only` commit and remained staged afterward untouched.
2. Claim commit's file list referenced a prototype file that doesn't exist yet at claim time → claim and completion now have separate explicit allowlists (claim: QUEUE.md + NIGHTLY-LOG.md only).
3. `node test-sanitizer.mjs || echo ...` masks failure as shell success (echo's exit code, not the test's) → rewritten as `if ! node ...; then ... exit 1; fi` for both sanitizer and postMessage self-tests.
4/5. Lock directory could exist without owner.json (race between mkdir and write); expired-lock takeover was non-atomic and the "write new owner.json" step was literally a placeholder comment, never real code → rewrote as `lock-acquire.sh`: verifies its own owner.json write landed before declaring success; treats an ownerless lock dir as live (not a takeover target, to avoid guessing through a genuine crash-vs-in-progress ambiguity); expired takeover quarantines (mv) the stale dir before retrying mkdir. Tested 7 scenarios against a disposable lock dir: fresh acquire, blocked-while-live, wrong-runid release refused, correct release, re-acquire after release, expiry takeover produces new run id, ownerless dir correctly treated as live.
6. No trap guaranteed lock cleanup on early/interrupted exit → `lock-release.sh` verifies runId ownership before removing (prevents a late/confused release from nuking a different, legitimately newer run's lock); AGENT-PROMPT.md now states every exit path must call it, and `lock-acquire.sh` traps its own narrow acquisition window.
7. `git status --porcelain` + substring slicing breaks on filenames with spaces/quotes and mis-parses renames → `git-baseline.sh` uses NUL-delimited `git status --porcelain=v1 -z`, explicit rename-record handling. Tested 6 scenarios including a literal "weird file.txt" and a real `git mv` rename — all correct, no crash, no false negative.
8. No check that HEAD is actually `main` before commit/push → `guarded-commit-push.sh` refuses if `git symbolic-ref --short HEAD` isn't exactly `main`. Tested: commit on a feature branch is refused before touching anything.
9. Sanitizer's `url()` check used `.match()` (first occurrence only) — `fill="url(#safe) url(https://evil)"` slipped through → changed to `matchAll`, every occurrence checked. New adversarial fixture added and confirmed caught.
10. Sanitizer walked only `root` but serialized the whole `doc` — a processing instruction (`<?xml-stylesheet href="https://evil">`) outside the root would survive into output → now explicitly rejects any non-whitespace/comment top-level sibling of root, AND serializes only `root`, not `doc` (belt and suspenders). New fixture confirmed caught.
11. No check that the SVG root is actually in the SVG namespace → added explicit `namespaceURI` check. New fixture (bogus namespace on a tag literally named "svg") confirmed caught.
12. postMessage "strict schema" accepted objects with extra keys beyond `{type, nonce}` → tightened to exact-key-count check. New adversarial test cases added (extra key, array-as-data) and confirmed rejected. (One of my own over-corrections — an added cross-realm-prototype check — caused a false failure in the test harness itself and was removed since it doesn't correspond to any real exploit under genuine postMessage structured-clone semantics; caught by actually running the tests, not assumed correct.)
13. Gallery marked an iframe "loaded" synchronously on `src` assignment, before the real `load` event — a fast scroll-past during initial load could send a 'pause' message before the child's listener existed, losing it, leaving the animation running indefinitely offscreen → added real `ready` state gated on the iframe's `load` event, with pending-intent queuing/flushing.
14. Gallery leaked an `IntersectionObserver` per card on every filter-input keystroke (re-render never disconnected old ones) → observers now stored per card state and disconnected before every re-render.
15. Manifest `file` values were used as iframe `src` with no validation — a corrupted/malformed manifest could point at an external URL or a traversal path → added strict local-filename regex validation in the gallery before ever setting `iframe.src`; invalid entries render an explicit warning placeholder instead of loading.
16. The written instructions literally said "escape `<` to `<`" — a no-op that changes nothing, in both `manifest.js`'s header comment and AGENT-PROMPT.md → corrected to the actual escape sequence `<`, spelled out unambiguously in both places.
17. No independent verification that manifest.js is actually pure data — the gallery's array-type check happens AFTER the file has already executed as a script, too late → built `concepts/tools/validate-manifest.mjs`: parses the source text (never executes it), confirms it's exactly comments + one `window.MANIFEST = <JSON>;` assignment, validates every field/status/filename, rejects duplicate ids. Tested 10 scenarios (valid, script-tag breakout attempt, function-call substitution, appended second statement, path traversal, external-URL file, duplicate ids, invalid null-file/status combination) — all correct. Wired into AGENT-PROMPT.md as a mandatory pre-commit gate AND a preflight sanity check on the existing file.
18. **The architectural finding**: asking a memory-less nightly agent to reproduce fragile multi-line git/lock bash from prose every night is itself the source of most of the above bugs → extracted all of it into committed, tested scripts (`lock-acquire.sh`, `lock-release.sh`, `git-baseline.sh`, `guarded-commit-push.sh`, `validate-manifest.mjs`) that AGENT-PROMPT.md now calls by name instead of reconstructing. AGENT-PROMPT.md itself rewritten to reflect this throughout Steps 0, 2, 6, 7, and the Stuck Protocol.

All fixes verified with real adversarial test cases (sanitizer: 17/17 fixtures; postMessage contract: 9/9 simulated exchanges; manifest validator: 10/10 scenarios; lock scripts: 7/7 scenarios against a disposable lock dir; git-baseline: 6/6 scenarios including spaces and renames; guarded-commit-push: 5/5 scenarios against a disposable repo+remote including a genuine non-fast-forward rejection) — not asserted, run.

## Codex code-level review (round 6, same thread) — a second implementation pass after round 5's fixes landed
VERDICT: REVISE. 9 findings, all real, none rejected. Same discipline as round 5: every fix written AND tested against real scenarios before being trusted, not patched on inspection alone — this round specifically caught two bugs IN my own first-attempt fixes (below), both only found by actually running the tests.

### Claude's response — all 9 accepted and fixed, each verified:
1. The round-5 `<` → `<` escaping fix had NOT actually landed — both `AGENT-PROMPT.md` and `manifest.js` still contained the literal bare `<` character despite the prose describing the correct fix. My own tool-call composition had silently mis-transcribed the six-character escape sequence twice (once originally, once in a "fix" that didn't take) → corrected for real via a Python script run through bash (to guarantee literal byte output rather than risk a third silent mis-transcription); verified with `grep -c '<'` showing exactly 1 occurrence in each file.
2. **Baseline deletion-detection gap**: a file CLEAN (untracked-by-git-status) at `save` time that gets deleted before `check` time was invisible to the diff — `hash_file` produced literally no output for a nonexistent path, so both snapshots had zero lines for it, and "absent vs. absent" looked identical → `hash_file` in `git-baseline.sh` now emits an explicit `MISSING  <path>` sentinel line whenever git status reports a path that no longer exists on disk, regardless of whether that path was ever in the baseline. Tested against a real disposable git repo: a clean out-of-scope file, baselined, then deleted — `check` now correctly exits 1 and shows the `MISSING` line as the diff.
3. **Baseline race + global shared path**: `git-baseline.sh save` ran BEFORE `lock-acquire.sh` in `AGENT-PROMPT.md`'s Step 0, and the baseline file lived at one fixed global path (`/tmp/nightly-baseline-hashes.txt`) — a second process could overwrite the first's baseline before the lock check even ran → reordered Step 0 so lock acquisition is step 5 and baseline save is step 6 (baseline now strictly after the lock, which is what actually serializes runs); independently, `git-baseline.sh` now requires a `run_id` argument and scopes its file to `/tmp/nightly-baseline-hashes.<run_id>.txt` as defense in depth even if the ordering were ever violated again. `guarded-commit-push.sh` now also takes `run_id` as its first argument and forwards it to `git-baseline.sh check`, so the drift check always reads the same run's own snapshot. Tested: two different run_ids produce two independently-existing baseline files that don't collide.
4. **Ownerless-lock permanent brick**: a crash landing in the narrow window between `mkdir` and the `owner.json` write left a lock directory with no owner file and no `expiresAt` to check — the old code treated this as live forever, every night, with no recovery path short of a human manually removing the directory → `lock-acquire.sh` now uses the lock directory's own mtime as a short (300s) grace-period clock for the ownerless case specifically: within the window it's treated as a genuine in-progress acquisition (live); past it, it's provably an abandoned crash remnant and gets quarantined-and-taken-over, same mechanism as the existing expired-owned-lock path. Tested 6 scenarios end-to-end against a disposable lock dir (fresh ownerless dir correctly blocks; a 10-minutes-old ownerless dir is correctly taken over; release/re-acquire/live-block/expired-owned-takeover all still pass, no regression). Caught a real cross-platform bug while building the fix: GNU `stat -f` (Linux "filesystem mode") and BSD/macOS `stat -f` (format flag) are entirely different flags: the GNU form prints a multi-line filesystem report to stdout AND still exits nonzero on an unrecognized `%m` directive, and the original one-liner (`stat -f %m ... || stat -c %Y ...` inside a single `$(...)`) captured BOTH branches' stdout concatenated together instead of the fallback replacing the failed attempt — corrupting the mtime into a multi-line string and crashing the arithmetic with an "unbound variable" error. Fixed by using two separate assignment statements chained with `||` instead of one substitution with an internal `||`. Confirmed via direct reproduction before and after the fix.
5. **Misleading no-op trap**: `trap 'true' EXIT INT TERM HUP` ran the command `true` on exit — literally nothing — while the surrounding comment claimed it "prevents this script from ever leaving a lock behind." Documenting the no-op honestly would have been the minimum fix, but building a REAL cleanup trap instead: all three acquisition success-paths now install a trap that actually `rm -rf`s the lock directory the script just created if it dies before handing the run ID back, then explicitly clear the trap (`trap - EXIT INT TERM HUP`) right before the deliberate final `exit 0` so the normal successful path never fires it. Tested by injecting a simulated crash (`exit 1`) right after trap installation in a disposable copy of the script: first attempt used `rmdir` and the lock directory was NOT cleaned up (rmdir silently no-ops on a non-empty directory, and owner.json is already written by that point) — caught by actually running the test, not by inspection; fixed to `rm -rf`, re-tested, confirmed the lock directory is genuinely removed on the simulated crash and genuinely left intact on the normal successful path.
6. **Stuck protocol didn't durably commit blocked state**: setting the queue/manifest entry to `blocked` only touched local files — no commit, no push — so a genuinely blocked run was indistinguishable from a silent crash to the next run's preflight or to Ben checking in the morning → Stuck Protocol now calls `guarded-commit-push.sh` with the same `$RUN_ID` to commit and push `QUEUE.md`/`NIGHTLY-LOG.md` before releasing the lock, with an explicit fallback (log-only, no commit attempt) for the one case where there's no `RUN_ID` yet to protect it — a preflight failure before lock acquisition itself.
9. **Push outcome not machine-readable**: `guarded-commit-push.sh` only narrated its outcome in English to stderr, so `NIGHTLY-LOG.md`'s "commit sha"/timing fields depended on the agent correctly parsing prose rather than reading a defined value → script now prints exactly one line to stdout on every exit path, `RESULT: <status> sha=<sha-or-none>`, with `status` one of `pushed` / `committed-not-pushed-day-work` / `push-rejected` / `nothing-to-commit`; `AGENT-PROMPT.md` now instructs the agent to log the literal RESULT line's fields rather than paraphrase stderr. Tested all 4 states against disposable repos with a real fake remote, including a genuine non-fast-forward rejection reproduced with two independent clones racing to push (confirmed: no force-push, rejection correctly logged, commit preserved locally, `RESULT: push-rejected sha=...` printed correctly).

(Findings #7 and #8 from this same round — the manifest validator's keyword-heuristic false-positive and its duplicate-id/iteration-chain handling — were fixed and tested in the same pass; see the validator's own inline comments for the reasoning. Tested: the exact "spaceship window"/"new moon" false-positive case now passes, a real 3-iteration version chain (v1→v2→v3, shared id) now passes, a broken `supersedes` link is correctly rejected, a true duplicate `(id, iteration)` pair is correctly rejected, and a filename collision across different ids is correctly rejected — 6 new scenarios plus all 10 pre-existing scenarios re-confirmed with no regressions.)

All 9 findings from this round are now fixed and tested against real adversarial scenarios, disposable git repos with fake remotes, and direct trap/signal simulation — not assumed correct from reading the diff.

## Gemini code-level review (manual relay, first code-level pass — earlier rounds were plan-level only) — on lock-acquire.sh, lock-release.sh, git-baseline.sh, guarded-commit-push.sh, validate-manifest.mjs
VERDICT: REVISE. 7 findings, all real, none rejected outright — two were re-scoped after independent verification (see Fable pass below). Before implementing, ran the 7 findings past Fable (subagent, model: fable) as a contrarian pragmatism check — same role Fable played on AGENT-PROMPT.md's creative quality earlier — specifically to catch enterprise-grade hardening that's technically correct but wastes time solving a problem that essentially can't occur at this pipeline's actual scale (single nightly cron + occasional manual runs, private repo, no hostile third party). Fable's triage: fix #1/#2/#6 fully (real correctness bugs, not hardening), #3 as loud-fail-only (detect divergence, don't auto-rebase — an unattended agent healing a diverged branch it doesn't understand is worse than stopping), #5 for consistency (cheap, closes an inconsistency from earlier tonight's own fix), skip #4 (TOCTOU — compound improbability: needs two processes, an already-4h-expired lock, AND a sub-second race window), and #7 as either skip or a one-line whitelist (cheaper and more honest than escaping two hardcoded caller values).

### Claude's response — all 7 fixed and tested, two implemented beyond Fable's initial scope after investigation showed the cheaper/narrower fix didn't actually close the described bug:
1. `try_acquire`'s failure branch used `rmdir` (silently no-ops on a non-empty directory) instead of `rm -rf`, so a partially-written owner.json left a permanently wedged lock. Investigating further than the narrow fix revealed the ACTUAL permanent-wedge scenario Gemini described wasn't about `try_acquire`'s own cleanup at all — it was a separate dead-end: an owner file that exists but is unparseable (e.g. a process SIGKILLed mid-write — uncatchable, so even this script's own exit trap can't prevent it) hit a completely different branch than the "no owner file" case, and that branch had zero recovery mechanism (`exit 2` forever, no grace-window-then-takeover, unlike the ownerless case which already had one from tonight's earlier Codex-round fixes). Fixed by extracting a shared `attempt_broken_lock_takeover()` function and routing BOTH "no owner file" and "owner file present but malformed" through the identical mtime-based grace-then-takeover logic. Tested: fresh malformed-owner-file blocks (within grace), old malformed-owner-file is taken over (past grace) — plus full regression on the ownerless/expired-owned paths, no breakage.
2. `guarded-commit-push.sh` never enforced that commit messages start with `nightly:` — it just trusted every caller. A single non-prefixed commit would look exactly like Ben's day work to every future run's day-work-detection check, silently blocking all pushes forever. Fixed: the script itself now refuses (exit 1) any message not starting with `nightly: `, validated at the trust boundary between the LLM-driven caller and this deterministic script — matching Fable's framing that "trusts every caller" really means "trusts an LLM to follow prompt instructions forever," which isn't a safe assumption. Tested: non-prefixed message refused before any commit is made; correctly-prefixed message still works.
3. The push-rejection comment claimed a future run would "retry naturally" — false: `git fetch` updates the remote-tracking ref but never merges/rebases local main, so a future run just piles another commit onto the still-diverged branch and fails again, forever, invisibly. Fixed the comment, and (going one step past Fable's minimal suggestion after confirming with Fable's own final-pass approval) changed push-rejected from `exit 0` (silently logged, run continues normally) to `exit 1` — which plugs directly into the Stuck Protocol's existing nonzero-exit handling without needing new logic, since AGENT-PROMPT.md's Step 2 and Step 7 call sites already document "nonzero from this script is a stuck-protocol trigger." Deliberately did NOT add automatic pull/rebase — an unattended agent auto-resolving a divergence it doesn't understand is a worse failure mode than a loud, safe stop. Tested against two real cloned repos racing to push: confirmed no force-push, commit preserved locally, and — the actual behavior change — exit code 1 where it used to be 0.
4. TOCTOU race in expired-lock takeover: two processes evaluating the same expired lock; process A quarantines+deletes+reacquires; process B's OWN `mv` (unaware A already acted) sweeps up A's brand-new live lock instead of the stale one, since `mv` on a known path can't distinguish "the stale thing I evaluated" from "something else that landed here since." Implemented despite Fable's initial "defensible to skip" read, because the actual fix turned out cheap (~15 lines) rather than the "heavier machinery" Fable was weighing against: capture the expired lock's runId from the SAME single read that produces its expiresAt (not a fresh re-read, which would just reopen an equivalent gap at a different point — caught this while re-reading my own first-draft fix, before ever testing it), then after quarantining, verify the quarantined directory still holds that exact runId before treating it as safe to delete; a mismatch restores it and aborts rather than destroying someone else's live lock. Tested against a REAL race, not a simulation: launched an actual background process with an injected `sleep` positioned precisely before its own `mv` (the first attempt at this test put the sleep in the wrong place — after the mv instead of before — and passed for the wrong reason, via `try_acquire`'s pre-existing atomic-mkdir safety net rather than the new check; repositioning the sleep to before the `mv`, matching Gemini's actual described race, is what genuinely exercised the new mismatch-detection code path), while a second real process fully completed its own takeover of the same original stale lock in the gap. Confirmed: the mismatch was detected, the live lock was restored, and the racing process aborted safely (exit 2) instead of stealing it.
5. `git-baseline.sh`'s `CURRENT_FILE` was still a single hardcoded global path even after `BASELINE_FILE` was scoped by run_id earlier tonight — the exact same collision class the scoping fix was meant to close remained open for the check-time snapshot. Fixed to `/tmp/nightly-current-hashes.<run_id>.txt`, matching `BASELINE_FILE`'s pattern. Tested: two different run_ids' check calls produce independent files, no collision.
6. `validate-manifest.mjs`'s iteration-chain check required `supersedes` be truthy for any iteration > 1, but if the prior iteration was `blocked` (file: null — a legitimate state), the correct `supersedes` value for the next iteration is also null (nothing to point at) — which the truthy-check rejected outright. This made it mathematically impossible to validly supersede a blocked/file-less iteration with real content, rejecting a completely ordinary sequence (a blocked night followed by a real retry). Fixed: replaced the truthy pre-check with a `.has()`-based existence check (distinguishing "no entry for iteration N-1 at all" from "an entry exists and its file is legitimately null") plus exact equality comparison, normalizing `undefined`/`null` consistently. Tested 3 new scenarios (blocked iteration1 correctly superseded by real iteration2 — now passes; iteration2 wrongly claiming a non-null supersedes over a blocked prior — correctly rejected; a chain with a missing link, iteration 1 jumping straight to 3 — correctly rejected with a distinct "chain is missing a link" message) plus full regression on the pre-existing 14 scenarios, no breakage.
7. `lock-acquire.sh` interpolated `$TRIGGER` unescaped into `owner.json`'s JSON string; a value containing a literal double-quote would produce invalid JSON. Per Fable's framing (a whitelist is cheaper and more honest than escaping a value that only ever legitimately takes two forms), replaced escaping with a strict whitelist (`scheduled`/`manual`/`unspecified`) that refuses anything else outright. Tested: a trigger value containing an embedded quote is refused before any lock directory is even created; a whitelisted value still produces valid, parseable JSON (checked with `python3 -c "import json; json.load(...)"`, not just eyeballed).

**Post-implementation Fable check (same subagent thread continued, final-pass review):** presented the two places implementation diverged from the initial triage (finding #1's broader scope, finding #4 getting built instead of skipped) plus finding #3's exit-code change, asking directly whether any of it was wrong, over-scoped, or created a new problem. Verdict: sound, ship it — with two specific things flagged to verify rather than just accept: (a) whether the grace-window takeover re-reads owner.json fresh rather than trusting a stale parse, so a legitimately slow-but-alive writer finishing mid-grace-window gets recognized rather than evicted; (b) whether Stuck Protocol's blocked state gates the *next* scheduled run indefinitely, or whether a fresh run can self-heal once the underlying problem (e.g. divergence) is actually resolved by a human. Both independently verified rather than taken on trust: (a) confirmed empirically that creating a file inside a directory bumps that directory's mtime on this filesystem, and traced that `try_acquire` always `mkdir`s fresh before `write_owner_file` ever runs — meaning owner.json is structurally always a brand-new file, never an in-place overwrite, so a legitimate slow writer's completion always refreshes the mtime the grace window reads; (b) traced that Stuck Protocol's `blocked` status is scoped to the specific queue entry, not a global gate — the lock is released unconditionally at the end, so a fresh run the next night can claim a different queue entry immediately, and would only hit the identical push-rejection again if the real divergence remains genuinely unresolved, which is the intended "loud every night until fixed" behavior rather than a silent, permanent brick.

Full fresh verification run (all commands re-run in this same pass, not relying on earlier scattered test output): `validate-manifest.mjs` 17/17 fixture scenarios; `lock-acquire.sh`/`lock-release.sh` 11/11 scenarios including the malformed-owner-file recovery and the trigger whitelist; the TOCTOU race reproduced under genuine background-process concurrency with a correctly-positioned injected delay; `guarded-commit-push.sh` all 5 outcome states (`pushed`, `committed-not-pushed-day-work`, `push-rejected` now exiting 1, `nothing-to-commit`, and the new prefix-refusal) against real disposable git repos with a real remote; `git-baseline.sh` deletion detection and `CURRENT_FILE` run_id-scoping. All 5 modified files pass `bash -n`/`node --check` syntax validation.
