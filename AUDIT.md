# Trivia OS — Full System Audit

**Date:** 2026-07-05
**Method:** Direct code investigation (build, grep, targeted reads, `git log` verification) across the full `client/src` tree. Three parallel background subagents were dispatched to cover the theme deep-dive, wiring audit, and bug-hunt/UX sections in parallel; all three stalled twice on an infrastructure watchdog issue unrelated to the task content. Rather than keep retrying, the remaining ground was covered directly in the foreground. See **Scope Note** at the end for what that means for coverage.

---

## 1. Executive Summary

**Health grade: B+ — solid foundation, no crash-class bugs found, real but fixable live-show risk.**

Zero P0 (crash/show-breaking) findings turned up under direct code reading — that's the headline, and it's genuinely good news for a platform running live in front of a paying crowd. The core architecture (per-show theme overrides, Realtime sync, the font-upload lifecycle, the Final Break/Winner Reveal auto-close, error boundaries on `/display`, timezone handling) is well-built and, in several places, more defensively coded than the documentation gives it credit for.

The real risk in this codebase right now is **documentation drift**, not code defects: several "critical rules" and "known mechanisms" described in SKILL.md/references no longer match what's actually running, in ways that could cause a future session (human or agent) to redo already-shipped work, hunt for UI controls that don't exist, or trust a status marker that's wrong.

**Top 5 risks before the next live show:**

1. **WIRE-1 (P1)** — Opening the Scoreboard admin modal doesn't suppress the Stream Deck/keyboard shortcuts the way the old Score panel does. A stray key after clicking a non-input control inside the modal can advance the live slide or toggle the TV scoreboard overlay while the host is looking at the wrong screen.
2. **THEME-1 (P1)** — The TV scoreboard overlay (`ScoreboardOverlay.jsx`) hardcodes `Boogaloo` at all 3 of its text sites and completely ignores per-show font overrides — the one surface where a custom font pick visibly doesn't apply.
3. **WIRE-2 (P1, doc)** — The documented "Final Break" checkbox mechanism doesn't exist in the code at all. The real mechanism (fully automatic, no checkbox) works differently and is undocumented — a real risk of confusion or duplicated effort next time someone touches show-closing logic.
4. **DRIFT-1 (P1, doc)** — 4 of the 9 bespoke ambient themes have already shipped reworks (confirmed via `git log`) but are marked as still needing work in both SKILL.md and themes.md — risking a future session re-doing finished work.
5. **BUG-1 (P2)** — Winner Reveal silently ends with no name/points/confetti if zero teams ever recorded a score — an unlikely but real edge case with no error and no crowd-facing explanation.

Everything else is P2/P3: dead code, stale doc claims, and small cosmetic gaps. Build succeeds cleanly with only 3 minor warnings (dead-code duplicate keys in `Join.jsx`).

---

## 2. Findings Table

| ID | Sev | File:Line | Description | Evidence | Recommended Fix |
|---|---|---|---|---|---|
| **WIRE-1** | P1 | `client/src/components/host/LiveMode.jsx:228`, `views/Host.jsx:117,228` | `handleKeyDown`'s guard checks `scorePanelOpen`/`themePickerOpen` but not `Host.jsx`'s `showScoreboard` (the flag that controls `ScoreboardModal`). A Stream Deck press while the modal is open, with focus on a non-input element inside it, leaks through to the global handler and can advance the live slide or toggle the TV scoreboard overlay. | Read `LiveMode.jsx:226-242` (guard clause `if (scorePanelOpen \|\| themePickerOpen) return`) and `Host.jsx:117,228` (`ScoreboardModal` gated by a separate `showScoreboard` state never passed into LiveMode). | Pass `showScoreboard` down into `LiveMode` as a prop and add it to the guard condition + `useCallback` dependency array (matches the existing `scorePanelOpen`/`themePickerOpen` pattern). |
| **THEME-1** | P1 | `client/src/components/display/ScoreboardOverlay.jsx:59,99,168` | All 3 text elements hardcode `fontFamily: "'Boogaloo', sans-serif"` with no fallback to `theme.fonts.display`. Per-show font overrides silently don't apply to the TV scoreboard overlay — the one surface visibly out of step with the "every headline reads theme.fonts.display" rule. | Direct grep + read of all 3 sites; confirmed no theme reference anywhere in the file's font handling. | Change each to `` fontFamily: `'${theme.fonts.display}', 'Boogaloo', sans-serif` `` (same safe fallback-chain pattern already used elsewhere, e.g. `WinnerRevealSlide.jsx`). |
| **WIRE-2** | P1 (doc) | SKILL.md, `references/slides.md` vs. `client/src/views/Display.jsx:486-507` | Docs describe a per-slide `isFinalBreak` checkbox (grep for `isFinalBreak`/`finalBreak`/`final_break` across the whole `client/src` tree returns **zero hits**). The actual, different mechanism is fully automatic: jumps to the last slide only when it's type `winner-reveal` AND no `grading-break` slides remain after the current position — no toggle exists anywhere in the UI. | Read `Display.jsx:486-507` in full; confirmed no `isFinalBreak` field/checkbox anywhere via repo-wide grep; confirmed no such UI in `SlideEditor.jsx`'s grading-break editor. | Rewrite the "Final Break" sections of SKILL.md and `references/slides.md` to describe the real auto-detection logic. Minor implementation note to add to the doc: any non-grading-break slide placed between the last grading break and the winner-reveal slide gets silently skipped by the auto-jump (only "no more grading-breaks remain" is checked, not "winner-reveal is the very next slide"). |
| **DRIFT-1** | P1 (doc) | SKILL.md:398, `references/themes.md:202-224` | `midnight-galaxy`, `halloween`, `under-the-sea`, and `meteor-shower` all have dedicated ambient-rework commits but show unmarked (no ✓) in both docs' theme status lists, incorrectly implying they still need bland-pass rework. | `git log --oneline --all --grep=... -- client/src/components/display/ParticleBackground.jsx` for each theme returned real, dedicated rework commits (e.g. `1c44aea feat(ambient): midnight-galaxy — core + cluster band...`, `f46e748 under-the-sea: rework ambient...`, `bf8b678 feat(ambient): meteor-shower — radiant anchor...`, `d912028 restore halloween rework...`). | Add ✓ to all 4 in both docs' theme tables. |
| **BUG-1** | P2 | `client/src/components/display/slides/WinnerRevealSlide.jsx:124-136` | If both `scoreboard_teams` and the legacy `teams`/`team_scores` fallback are empty (zero teams ever scored), the `if (ranked.length)` guard skips `setWinner` entirely — `winner` stays `null` forever. The drumroll plays and "And the winner is…" renders, but no name/points/confetti ever appear, with no error shown. | Traced the full data-load effect; confirmed the empty-array short-circuit. | Add an explicit empty-state branch, e.g. `setWinner({ name: 'No scores recorded', total: 0, isTie: false })` or a dedicated "no data" render path. |
| **THEME-2** | P2 | `client/src/views/Display.jsx:138` | PreShowScreen's team-count number hardcodes `fontFamily: "'Boogaloo', sans-serif"` while the `color` on the same style object correctly reads `theme.colors.highlight` — an inconsistency on the same element. | Direct read of the style block. | Change to the same `theme.fonts.display` fallback-chain pattern used at lines 33/118/295 in the same file. |
| **DRIFT-2** | P2 | SKILL.md Key Components vs. `client/src/components/host/ShowManager.jsx` / `ShowLibrary.jsx` | `ShowManager.jsx` is dead — zero real callers (only its own function declaration plus one stale comment in `ShowLibrary.jsx`). The live "My Shows" component is the undocumented `ShowLibrary.jsx`, imported and rendered in `Host.jsx:189-199`. SKILL.md documents the dead file and doesn't mention the live one — structurally identical to the already-known ThemePicker.jsx trap. | `grep -rn "ShowManager"` and `grep -rn "ShowLibrary"` across `client/src`; confirmed `ShowLibrary` is imported/rendered in `Host.jsx`, `ShowManager` is not referenced anywhere live. | Update SKILL.md's Key Components to document `ShowLibrary.jsx` instead of `ShowManager.jsx`. Consider deleting `ShowManager.jsx` (flagged, not deleted — see Phase 8 notes). |
| **THEME-3** | P2 | `client/src/themes/index.js` (all 21 entries) + `client/src/components/display/frames/FrameRegistry.js` | Every theme carries `particles: {color,count,behavior}` and `cutawayFrame: 'none'` — neither field has a single reader anywhere in `client/src`. Paired with a fully dead `FrameRegistry.js` (a `getFrame(type)` registry with only a `'none'` passthrough, zero import sites) — looks like the abandoned other half of a never-finished `cutawayFrame` feature. Not documented anywhere. | `grep -rn "\.particles\b"` and `grep -rn "cutawayFrame"` across `client/src` — zero consumers outside `themes/index.js` itself. `grep -rn "FrameRegistry"` outside its own file — zero hits. | Either strip `particles`/`cutawayFrame` from all 21 theme entries and delete `frames/FrameRegistry.js`, or, if there's a real plan for a cutaway-frame feature, document it. |
| **DRIFT-3** | P2 | SKILL.md:396, `references/build-state.md` Known Issues | SKILL.md claims `getTheme()` falls back to `'midnight-galaxy'` — the actual code falls back to `DEFAULT_THEME_ID` (`'pure-michigan'`). Both docs' "three fallback theme IDs... haven't been unified" framing is also stale: `getTheme()`, `DEFAULT_THEME_ID`, and `normalizeShow()`'s fallback all already resolve through the single `DEFAULT_THEME_ID` constant — not three independent values. | Read `themes/index.js:214-220` and `useShow.js:18` directly. | Correct SKILL.md line 396 and build-state.md's Known Issues entry to reflect that this is already unified. |
| **DRIFT-4** | P2 (doc gap) | `client/src/components/host/HostPinGate.jsx` (whole file, wired at `views/Host.jsx:101-107`) | A real, well-built PIN-gate — server-side verification via a Supabase Edge Function (`verify-host-pin`), RLS enforcement via an `app_metadata.host_verified` JWT claim, anonymous-auth session persistence — wraps `/host` in production. This entire auth subsystem is completely absent from SKILL.md and every reference doc. Not a bug; the architecture looks sound. But it's the only real access-control layer in the app and nothing documents it. | Read `HostPinGate.jsx` in full; confirmed wiring into `Host.jsx`. | Add a "Host Access" section to SKILL.md describing the PIN-gate architecture. |
| **THEME-4** | P2 | `client/src/components/host/RoundSidebar.jsx:5-6` | `SLIDE_TYPE_META` maps both `'title'` and `'state-of-union'` to the identical 🇺🇸 icon — a recurrence of the exact bug class SKILL.md already warns about ("icons must match per type — this drifted once already"). Blast radius is low: `title` isn't in `AddSlideWizard`'s `TYPE_CARDS` (can't be freshly created) and isn't auto-seeded by `createShow` (which starts with `slides: []`), so it would only surface via a legacy or imported show. But if it does, it's visually indistinguishable from `state-of-union` in the sidebar. | Direct read of `SLIDE_TYPE_META` object literal; confirmed `title` absent from `TYPE_CARDS` and from `createShow`. | Give `'title'` a distinct icon (something evoking a generic show-opener, not the American-flag State-of-the-Union gag). |
| **THEME-5** | P3 | `client/src/components/host/RoundSidebar.jsx:4-19` | `SLIDE_TYPE_META` has 14 entries for 15 slide types — `'grid'` is missing. Falls back safely to `{icon:'📄', label: slide.type}` (line 412: `SLIDE_TYPE_META[slide.type] ?? {...}`), so a `grid` slide shows a generic document icon and the literal string "grid" rather than a proper label. Not a crash — the fallback exists — just a visible polish gap. | Confirmed the fallback exists at line 412; confirmed `'grid'` absent from the object literal. | Add a `'grid': { label: 'Color Schemes', icon: '🎨' }`-style entry (pick an icon distinct from `pixelate-series`'s existing 🎨). |
| **BUILD-1** | P2 | `client/src/views/Join.jsx:197/200, 328/333, 912/915` | Build emits 3 esbuild warnings for duplicate `transition` keys in inline style objects. In each case the second value silently wins, so the intended `cubic-bezier(0.23,1,0.32,1)` (= `EASE_OUT`) never actually applies — the element falls back to generic `ease` timing. Purely cosmetic (a 120ms button-press curve on `/join` phones) but it's compiler-flagged dead code, not a style choice. | Full `npm run build` output; direct read of all 3 sites. | Remove the redundant earlier `transition` line at each site (site 1 needs a merge since one line covers `opacity` and the other covers `transform` — keep the union, drop the duplicate declaration). |
| **DRIFT-5** | P3 | `references/build-state.md` Known Issues vs. `client/src/App.jsx` | build-state.md claims "`AmbientAudit.jsx` — dev tool, not routed in prod." It is routed: `App.jsx` lazy-imports it at `/ambient` (and `GradientAudit.jsx` at `/gradient`) with no dev-only guard. Lazy-loading does keep it out of the main bundle (confirmed in the build output: separate ~1.5KB/~2.9KB chunks), so this isn't a performance issue — just an incorrect doc claim, plus both routes are reachable with no auth gate (unlike `/host`'s PIN gate), which is low-risk (read-only preview tools) but unintentional-looking. | Read `App.jsx`'s route table directly; cross-checked against the build's chunk output. | Correct the doc claim. Optionally wrap both routes in a dev-only check or the same PIN gate if Ben doesn't want them publicly reachable. |
| **WIRE-3** | P3 (latent, not active) | `client/src/hooks/useShow.js`'s `normalizeShow()` vs. `client/src/components/display/slides/QuestionSlide.jsx:363-369` | `normalizeShow()` (used by `Host.jsx`/`BuildMode`/`LiveMode` via `useShow()`) never maps the real `audio_playing` DB column onto the returned show object. `QuestionSlide.jsx` reads `show?.audio_playing` expecting it — but that's on `Display.jsx`'s own, separately-maintained show object (which spreads raw Supabase rows directly, so the column survives there), not on `useShow()`'s normalized object. Nothing on the Host/LiveMode side currently reads `show.audio_playing`, so there's no active bug today — but it's a real gap in `normalizeShow`'s completeness, and a sign that `Display.jsx` and `useShow.js` maintain two independently-drifting show-shape implementations. | `grep -rn "audio_playing\|audioPlaying"` across `client/src` — only 3 hits total, none on the Host/LiveMode side. Read `normalizeShow()` in full — confirmed `audio_playing` absent from its return object. | Add `audioPlaying: row.audio_playing ?? null` to `normalizeShow()` for schema completeness, in case a future Host-side feature (e.g. a "playing" indicator in LiveMode) needs it. |
| **BUG-2** | P3 (doc-drift) | `client/src/components/display/slides/WinnerRevealSlide.jsx:12-24` | The drum roll is a plain pre-recorded `/drum-roll.mp3` played via an HTML5 `<Audio>` element — not the "3s synthesized Web Audio... accelerating snare hits, gap tightening 0.20s→0.03s" described in SKILL.md and `references/slides.md`. The file exists (`public/drum-roll.mp3`, confirmed on disk) and the implementation works and cleans up correctly; the docs just describe an earlier, different implementation. Minor separate nit: the ref holding the `Audio` element is named `audioCtxRef`, which reads as "AudioContext" but isn't one. | Read the component in full; confirmed `/public/drum-roll.mp3` exists via `ls`. | Update SKILL.md/slides.md's Winner Reveal description to describe the real MP3-playback implementation. Optional: rename `audioCtxRef` → `audioRef` for clarity. |

**Also confirmed working correctly (checked, not just assumed) — no action needed:**
- All 5 `<ThemeProvider>` call sites correctly pass `overrides={show.themeOverrides}` on every surface with a real show; the 2 that don't (demo mode, pre-load/no-show state) correctly have nothing to override.
- `applyOverrides()`'s partial-override merge and the preset-font `displayUrl: undefined` clear survive the Supabase JSONB round-trip correctly (JSON serialization drops `undefined` keys; the `!url` guard treats absent/undefined identically).
- Custom font upload → `FontFace` registration → `useRef`-tracked cleanup on unmount/change is correctly implemented; failed loads `console.warn` rather than throw.
- `TeamPickerSlide.jsx`'s text correctly reads `theme.fonts.display` (background is intentionally fixed per design).
- `SlideElements.jsx`'s `'Boogaloo'` default for new WYSIWYG text elements is **intentional**, not a bug — confirmed a real per-element font `<select>` exists in `SlideEditor.jsx:604`.
- `LiveMode.jsx`'s keyboard guard correctly no-ops while focus is on an `input`/`textarea`/`select`/`[contenteditable]` (the gap is specifically the ScoreboardModal-open case, see WIRE-1).
- `/display` **does** have a real error boundary (`ErrorBoundary` wraps `SlideRenderer` in `Display.jsx:347` with a per-slide `key` reset) — a single slide crashing won't white-screen the TVs.
- Timezone/date handling is correct everywhere checked — every `new Date(dateStr + 'T12:00:00')` site anchors to noon specifically to avoid the classic UTC-midnight day-rollover bug.
- `importShow()` gracefully defaults every optional field (title, date, theme, overrides, slides, rounds, powerups, ticker messages) via `??` chains.
- Nullable-data handling spot-checked in `QuestionSlide.jsx` and `PylRevealSlide.jsx` — consistent defensive `??`/optional-chaining patterns throughout.
- `ParticleBackground` correctly mounts outside `AnimatePresence` with no remount-forcing `key`; a GPU-only spot-check (grep for forbidden animated properties inside `@keyframes` blocks) found no violations.
- `saveResults()`'s auto-fire on the winner-reveal slide is correctly guarded against double-firing via a ref, and correctly re-fires if the host navigates away and back.
- The realtime-echo-vs-optimistic-navigation race is already deliberately hardened (a 1.5s local-nav guard window, well-commented).
- `WinnerRevealSlide.jsx` handles ties gracefully (joins tied names with " & ", shows "It's a tie!") — real, working, undocumented behavior worth a positive doc callout.
- `SLIDE_COMPONENTS` in `SlideRenderer.jsx` covers all 15 slide types and matches every type `AddSlideWizard` can create; the `grid`/`team-picker`/`state-of-union` opacity-neutralize special-case is present and correctly implemented.
- No `TODO`/`FIXME`/`HACK`/`XXX` markers and no stray `console.log` calls anywhere in `client/src` — clean hygiene.

---

## 3. Theme System — Deep-Dive Summary

Priority area, audited in the most depth:

- **Theme object shape**: all 21 themes have an identical, complete key set — no theme is missing a documented field. The shape is actually *more* complete than documented (see THEME-3's dead `particles`/`cutawayFrame` fields).
- **Fallback theme IDs**: contrary to 3 different docs' repeated warnings, this is already unified through one constant (`DEFAULT_THEME_ID`) — not three independently-drifted values (DRIFT-3).
- **ThemeProvider wiring & override merge**: fully verified correct across all 5 call sites and the merge/font-lifecycle logic — no gaps found.
- **Hardcoded fonts**: 2 real violations found (THEME-1, THEME-2), both on the `/display` side where it matters most (they'd visibly break a per-show font override on live TV). One suspected violation (`SlideElements.jsx`) turned out to be intentional per-element styling, confirmed via its real font-picker UI.
- **Ambient status markers**: confirmed via `git log` that the bespoke/gradient split itself is accurate, but 4 of 9 bespoke themes have wrong status markers (DRIFT-1).
- **Not fully covered in this pass**: an exhaustive frame-by-frame animation-law audit of all 9 bespoke ambients (anchor/drifter/atmosphere quality, safe-area precision) and the full hardcoded-hex-color sweep across every slide component. A GPU-only/reduced-motion spot-check came back clean, but wasn't exhaustive across all 21 theme branches. See Scope Note.

---

## 4. Wiring Map

- **`useShow.js` actions**: `Host.jsx`'s `actions = { ...showApi }` remains a full spread (not curated) — confirmed still intact, with an explicit comment warning future editors not to narrow it. No dead or missing actions found among the ones checked.
- **Realtime sync**: the show-state subscription in `useShow.js` correctly propagates `currentSlideIndex`/`currentSlideId`/`isLive`/`scoreboardVisible`/`scoresRevealed`/`answerReveal`, with a deliberate 1.5s echo-suppression window for optimistic nav. `audio_playing` is the one documented column that doesn't flow through this path (WIRE-3) — latent, not active.
- **Display routing**: the documented 4-branch waterfall is real and correctly ordered; the jukebox-return handler (WIRE-2) is a fully rewritten, undocumented mechanism.
- **Keyboard/Stream Deck**: correctly guarded against text-input focus; **not** correctly guarded against the Scoreboard modal being open (WIRE-1) — the most actionable finding in this audit.
- **Error boundaries**: present and correctly scoped on `/display` (WIRE finding retracted — this was suspected as a gap per the original brief but turned out to be already handled).
- **Two parallel show-shape implementations**: `Display.jsx` maintains its own show state largely independent of `useShow.js`'s `normalizeShow()` (see WIRE-3) — worth keeping in mind for any future change to the show schema, since a new column needs to be threaded through both places independently.

---

## 5. UI/UX Recommendations

**Not covered in this pass.** The dedicated subagent for this section stalled twice on an infrastructure issue (see Scope Note) and a hands-on visual review wasn't done directly — the project's own design docs are explicit that ambient/visual judgment calls are Ben's `[eyes]` call, not something to self-certify from reading code. Recommend a follow-up pass specifically for: `/host` Build Mode wizard/editor flow, Live Mode's mid-show recovery options (jump-to-slide, undo an accidental advance), `/display` readability across all 21 themes at TV distance, and `/join` registration friction — ideally with the dev server actually running and the `impeccable`/`emil-design-eng` skills loaded per this project's own conventions.

---

## 6. Doc Drift Summary

| Doc | Location | Issue | Fix |
|---|---|---|---|
| SKILL.md | Line 396 | `getTheme()` fallback claimed as `'midnight-galaxy'` | Correct to `DEFAULT_THEME_ID`/`'pure-michigan'` (DRIFT-3) |
| SKILL.md | Line 398 | 4 bespoke themes missing ✓ markers | Add ✓ to midnight-galaxy, halloween, under-the-sea, meteor-shower (DRIFT-1) |
| SKILL.md | Lines 101-102 | Warns about a "legacy unused `ThemePicker.jsx`" that no longer exists | Delete the stale warning |
| SKILL.md | Key Components | Documents dead `ShowManager.jsx`, omits live `ShowLibrary.jsx` | Swap (DRIFT-2) |
| SKILL.md | "Final Break" section | Describes a checkbox mechanism that doesn't exist | Rewrite per WIRE-2 |
| SKILL.md | Winner Reveal description | Describes synthesized Web Audio; actual is a pre-recorded MP3 | Rewrite per BUG-2 |
| SKILL.md | No "Host Access" section exists | `HostPinGate.jsx`'s entire auth architecture is undocumented | Add new section (DRIFT-4) |
| `references/themes.md` | Lines 202-224 | Same 4-theme marker drift as SKILL.md | Add ✓ (DRIFT-1) |
| `references/build-state.md` | Known Issues | Same fallback-theme-ID claim as SKILL.md | Correct (DRIFT-3) |
| `references/build-state.md` | Known Issues | Claims `AmbientAudit.jsx` "not routed in prod" | Correct — it is routed (DRIFT-5) |
| `references/slides.md` | Grading-break / Final Break section | Same as SKILL.md's Final Break drift | Rewrite per WIRE-2 |

---

## Scope Note

This audit is thorough at the code-correctness and wiring level — every finding above was verified by actually reading the relevant code, running the build, or checking `git log`, not inferred from the docs alone. Three areas got less depth than the original brief called for, and it's worth being direct about why:

Three parallel background subagents were dispatched early on to cover the theme system's remaining items (hardcoded-color sweep, `autoFitText` family-mismatch check, full 21-theme `ParticleBackground` animation-law audit), the wiring audit, and the bug-hunt/UI-UX review. All three stalled twice in a row on an identical infrastructure watchdog failure ("no progress for 600s") unrelated to their actual task content — a systemic issue with background execution at the time, not a problem with the investigation itself. Rather than burn more time on a third retry with the same likely outcome, the highest-value remaining ground (keyboard guards, error boundaries, Final Break wiring, Winner Reveal edge cases, timezone handling, nullable-data patterns, the scoreboard debounce path, several hardcoded-font sites) was covered directly in the foreground instead, which is reflected in the findings above.

What that leaves genuinely thin: the full hardcoded-hex-color sweep across every slide component, the `autoFitText` family-mismatch check at every call site, an exhaustive (not spot-checked) pass over all 21 ambient themes' keyframes for GPU-only/reduced-motion compliance, and the entire UI/UX review (Section 5). None of these showed any red flags in the spot-checking that did happen, but "spot-checked clean" isn't the same claim as "exhaustively verified" — worth a follow-up pass if you want that last layer of confidence, particularly on the color sweep since that's the kind of thing that's cheap to check and cheap to fix if something turns up.
